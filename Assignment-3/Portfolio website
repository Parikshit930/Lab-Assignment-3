<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parikshit Chauhan | AI/ML Developer</title>
    <style>
        /* ==================== RESET & BASE STYLES ==================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            /* Light Theme Colors */
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --text-primary: #1a1a2e;
            --text-secondary: #4a4a68;
            --accent-primary: #6366f1;
            --accent-secondary: #8b5cf6;
            --card-bg: rgba(255, 255, 255, 0.8);
            --shadow: rgba(0, 0, 0, 0.1);
            --border: rgba(0, 0, 0, 0.1);
        }

        [data-theme="dark"] {
            /* Dark Theme Colors */
            --bg-primary: #0f0f1e;
            --bg-secondary: #1a1a2e;
            --text-primary: #e4e4e7;
            --text-secondary: #a1a1aa;
            --accent-primary: #818cf8;
            --accent-secondary: #a78bfa;
            --card-bg: rgba(26, 26, 46, 0.8);
            --shadow: rgba(0, 0, 0, 0.3);
            --border: rgba(255, 255, 255, 0.1);
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            line-height: 1.6;
            transition: background 0.3s ease, color 0.3s ease;
            overflow-x: hidden;
        }

        /* ==================== HEADER & NAVIGATION (FLEXBOX) ==================== */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            background: var(--card-bg);
            backdrop-filter: blur(1rem);
            border-bottom: 0.0625rem solid var(--border);
            transition: all 0.3s ease;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.25rem 5%;
            max-width: 75rem;
            margin: 0 auto;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: fadeInLeft 0.8s ease;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            color: var(--text-primary);
            text-decoration: none;
            font-weight: 500;
            position: relative;
            transition: color 0.3s ease;
        }

        /* Hover effect with underline animation */
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -0.3125rem;
            left: 0;
            width: 0;
            height: 0.125rem;
            background: linear-gradient(90deg, var(--accent-primary), var(--accent-secondary));
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-links a:hover {
            color: var(--accent-primary);
        }

        /* Theme Toggle Button */
        .theme-toggle {
            background: var(--accent-primary);
            border: none;
            color: white;
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            transition: all 0.3s ease;
        }

        .theme-toggle:hover {
            transform: rotate(180deg) scale(1.1);
            box-shadow: 0 0 1.25rem var(--accent-primary);
        }

        /* Mobile Menu Toggle */
        .menu-toggle {
            display: none;
            flex-direction: column;
            gap: 0.3125rem;
            cursor: pointer;
        }

        .menu-toggle span {
            width: 1.5625rem;
            height: 0.1875rem;
            background: var(--text-primary);
            transition: all 0.3s ease;
        }

        /* ==================== HERO SECTION (FLEXBOX) ==================== */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 8rem 5% 4rem;
            background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
            position: relative;
            overflow: hidden;
        }

        /* Animated background gradient circles */
        .hero::before,
        .hero::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
            animation: float 8s ease-in-out infinite;
        }

        .hero::before {
            width: 31.25rem;
            height: 31.25rem;
            background: var(--accent-primary);
            top: -9.375rem;
            right: -9.375rem;
        }

        .hero::after {
            width: 25rem;
            height: 25rem;
            background: var(--accent-secondary);
            bottom: -6.25rem;
            left: -6.25rem;
            animation-delay: 2s;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 50rem;
        }

        .hero h1 {
            font-size: clamp(2rem, 5vw, 3.5rem);
            margin-bottom: 1rem;
            font-weight: 800;
            animation: fadeInUp 1s ease;
        }

        .hero .tagline {
            font-size: clamp(1rem, 2.5vw, 1.25rem);
            color: var(--text-secondary);
            margin-bottom: 2rem;
            animation: fadeInUp 1s ease 0.2s backwards;
        }

        .hero .title {
            color: var(--accent-primary);
            font-size: clamp(1.125rem, 3vw, 1.5rem);
            font-weight: 600;
            margin-bottom: 0.5rem;
            animation: fadeInUp 1s ease 0.1s backwards;
        }

        .cta-buttons {
            display: flex;
            gap: 1.25rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease 0.4s backwards;
        }

        .btn {
            padding: 0.875rem 2rem;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
            font-size: 1rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-0.1875rem) scale(1.05);
            box-shadow: 0 0.625rem 2rem var(--shadow);
        }

        .btn-secondary {
            background: transparent;
            color: var(--accent-primary);
            border: 0.125rem solid var(--accent-primary);
        }

        .btn-secondary:hover {
            background: var(--accent-primary);
            color: white;
            transform: translateY(-0.1875rem);
        }

        /* ==================== SECTION COMMON STYLES ==================== */
        section {
            padding: 5rem 5%;
            max-width: 75rem;
            margin: 0 auto;
        }

        .section-title {
            font-size: clamp(2rem, 4vw, 2.5rem);
            text-align: center;
            margin-bottom: 1rem;
            font-weight: 700;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -0.625rem;
            left: 50%;
            transform: translateX(-50%);
            width: 5rem;
            height: 0.25rem;
            background: linear-gradient(90deg, var(--accent-primary), var(--accent-secondary));
            border-radius: 0.125rem;
        }

        .section-subtitle {
            text-align: center;
            color: var(--text-secondary);
            margin-bottom: 3rem;
            font-size: 1.125rem;
        }

        /* ==================== ABOUT SECTION (FLEXBOX) ==================== */
        .about-content {
            display: flex;
            gap: 3rem;
            align-items: center;
            margin-top: 3rem;
        }

        .about-image {
            flex: 1;
            min-width: 18.75rem;
        }

        .profile-placeholder {
            width: 100%;
            aspect-ratio: 1;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            border-radius: 1.25rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 6rem;
            color: white;
            box-shadow: 0 1.25rem 3.125rem var(--shadow);
            transition: transform 0.3s ease;
        }

        .profile-placeholder:hover {
            transform: scale(1.05) rotate(2deg);
        }

        .about-text {
            flex: 2;
        }

        .about-text p {
            font-size: 1.125rem;
            line-height: 1.8;
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
        }

        /* ==================== SKILLS SECTION (FLEXBOX) ==================== */
        .skills-container {
            display: flex;
            flex-wrap: wrap;
            gap: 1.25rem;
            justify-content: center;
            margin-top: 3rem;
        }

        .skill-card {
            background: var(--card-bg);
            backdrop-filter: blur(0.625rem);
            padding: 1.5rem 2rem;
            border-radius: 1rem;
            border: 0.0625rem solid var(--border);
            transition: all 0.3s ease;
            cursor: pointer;
            box-shadow: 0 0.25rem 0.9375rem var(--shadow);
        }

        .skill-card:hover {
            transform: translateY(-0.625rem) scale(1.05);
            box-shadow: 0 0.9375rem 2.5rem var(--shadow);
            border-color: var(--accent-primary);
        }

        .skill-card:hover::before {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: 1rem;
            padding: 0.125rem;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
        }

        .skill-card {
            position: relative;
        }

        .skill-card span {
            font-weight: 600;
            font-size: 1.125rem;
            color: var(--text-primary);
        }

        /* ==================== PROJECTS SECTION (CSS GRID) ==================== */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(18.75rem, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .project-card {
            background: var(--card-bg);
            backdrop-filter: blur(0.625rem);
            border-radius: 1.25rem;
            padding: 2rem;
            border: 0.0625rem solid var(--border);
            transition: all 0.4s ease;
            cursor: pointer;
            box-shadow: 0 0.5rem 1.875rem var(--shadow);
            position: relative;
            overflow: hidden;
        }

        /* Gradient overlay on hover */
        .project-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(99, 102, 241, 0.1), transparent);
            transition: left 0.6s ease;
        }

        .project-card:hover::before {
            left: 100%;
        }

        .project-card:hover {
            transform: translateY(-0.9375rem);
            box-shadow: 0 1.25rem 3.75rem var(--shadow);
            border-color: var(--accent-primary);
        }

        .project-icon {
            width: 3.75rem;
            height: 3.75rem;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            border-radius: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin-bottom: 1.5rem;
            transition: transform 0.3s ease;
        }

        .project-card:hover .project-icon {
            transform: rotateY(360deg);
        }

        .project-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .project-card p {
            color: var(--text-secondary);
            line-height: 1.7;
            font-size: 1rem;
        }

        /* ==================== CONTACT SECTION (FLEXBOX) ==================== */
        .contact-container {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 3rem;
        }

        .contact-card {
            background: var(--card-bg);
            backdrop-filter: blur(0.625rem);
            padding: 2.5rem 3rem;
            border-radius: 1.25rem;
            border: 0.0625rem solid var(--border);
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
            flex: 1;
            min-width: 15.625rem;
            box-shadow: 0 0.5rem 1.875rem var(--shadow);
        }

        .contact-card:hover {
            transform: translateY(-0.625rem);
            box-shadow: 0 1.25rem 3.125rem var(--shadow);
            border-color: var(--accent-primary);
        }

        .contact-icon {
            width: 4.375rem;
            height: 4.375rem;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin: 0 auto 1.5rem;
            transition: transform 0.3s ease;
        }

        .contact-card:hover .contact-icon {
            transform: scale(1.1) rotate(10deg);
        }

        .contact-card h3 {
            margin-bottom: 0.75rem;
            color: var(--text-primary);
            font-size: 1.25rem;
        }

        .contact-card a {
            color: var(--accent-primary);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        .contact-card a:hover {
            color: var(--accent-secondary);
        }

        /* ==================== FOOTER ==================== */
        footer {
            background: var(--bg-secondary);
            padding: 2rem 5%;
            text-align: center;
            border-top: 0.0625rem solid var(--border);
        }

        footer p {
            color: var(--text-secondary);
        }

        /* ==================== ANIMATIONS ==================== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(1.875rem);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInLeft {
            from {
                opacity: 0;
                transform: translateX(-1.875rem);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-1.25rem);
            }
        }

        /* ==================== MEDIA QUERIES (RESPONSIVE) ==================== */
        
        /* Tablet - 768px and below */
        @media (max-width: 48rem) {
            .nav-links {
                position: fixed;
                top: 5rem;
                left: -100%;
                flex-direction: column;
                background: var(--card-bg);
                backdrop-filter: blur(1rem);
                width: 100%;
                padding: 2rem;
                gap: 1.5rem;
                transition: left 0.3s ease;
                border-bottom: 0.0625rem solid var(--border);
            }

            .nav-links.active {
                left: 0;
            }

            .menu-toggle {
                display: flex;
            }

            .about-content {
                flex-direction: column;
            }

            .projects-grid {
                grid-template-columns: 1fr;
            }

            .hero {
                padding: 6rem 5% 3rem;
            }

            section {
                padding: 3rem 5%;
            }
        }

        /* Mobile - 480px and below */
        @media (max-width: 30rem) {
            html {
                font-size: 14px;
            }

            nav {
                padding: 1rem 5%;
            }

            .cta-buttons {
                flex-direction: column;
                width: 100%;
            }

            .btn {
                width: 100%;
            }

            .contact-container {
                flex-direction: column;
            }

            .skill-card {
                width: 100%;
            }

            .hero::before,
            .hero::after {
                display: none;
            }
        }

        /* Desktop - Large screens */
        @media (min-width: 75rem) {
            .projects-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        /* Scroll animation trigger */
        .fade-in {
            opacity: 0;
            transform: translateY(1.875rem);
            transition: opacity 0.6s ease, transform 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .profile-img {
  width: 220px;
  height: 220px;
  object-fit: cover;
  border-radius: 50%;
  border: 4px solid var(--accent-color);
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
  animation: float 4s ease-in-out infinite;
}
.about-image {
  background: rgba(255, 255, 255, 0.05);
  padding: 1.5rem;
  border-radius: 50%;
  backdrop-filter: blur(10px);
}

    </style>
</head>
<body>
    <!-- ==================== HEADER & NAVIGATION ==================== -->
    <header>
        <nav>
            <div class="logo">PC</div>
            <ul class="nav-links" id="navLinks">
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <div style="display: flex; gap: 1rem; align-items: center;">
                <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">🌙</button>
                <div class="menu-toggle" id="menuToggle">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            </div>
        </nav>
    </header>

    <!-- ==================== HERO SECTION ==================== -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Hi, I'm <span style="background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">Parikshit Chauhan</span></h1>
            <p class="title">B.Tech CSE (AI/ML) Student</p>
            <p class="tagline">"Blending Artificial Intelligence with clean code to build smart, responsive digital experiences."</p>
            <div class="cta-buttons">
                <a href="#projects" class="btn btn-primary">View My Work</a>
                <a href="#contact" class="btn btn-secondary">Get In Touch</a>
            </div>
        </div>
    </section>

    <!-- ==================== ABOUT SECTION ==================== -->
    <section id="about" class="fade-in">
        <h2 class="section-title">About Me</h2>
        <p class="section-subtitle">Passionate about AI, Robotics & Web Development</p>
        <div class="about-content">
  <div class="about-image">
    <img src="Parikshit.jpg" alt="Parikshit Chauhan" class="profile-img">
  </div>
</div>

            <div class="about-text">
                <p>
                    Parikshit Chauhan is a dedicated B.Tech Computer Science Engineering student specializing in Artificial Intelligence and Machine Learning, with a strong passion for building intelligent systems and modern web experiences.
                </p>
                <p>
                    He actively works on innovative technical projects that combine hardware and software, demonstrating a deep interest in robotics, automation, and responsive web development. Constantly driven by curiosity and creativity, he strives to design efficient solutions that merge technology with real-world practicality.
                </p>
            </div>
        </div>
    </section>

    <!-- ==================== SKILLS SECTION ==================== -->
    <section id="skills" class="fade-in">
        <h2 class="section-title">Skills & Expertise</h2>
        <p class="section-subtitle">Technologies I work with</p>
        <div class="skills-container">
            <div class="skill-card"><span>HTML5</span></div>
            <div class="skill-card"><span>CSS3</span></div>
            <div class="skill-card"><span>JavaScript</span></div>
            <div class="skill-card"><span>Python</span></div>
            <div class="skill-card"><span>AI/ML Basics</span></div>
            <div class="skill-card"><span>CSE Fundamentals</span></div>
            <div class="skill-card"><span>Responsive Design</span></div>
            <div class="skill-card"><span>Robotics Systems</span></div>
        </div>
    </section>

    <!-- ==================== PROJECTS SECTION ==================== -->
    <section id="projects" class="fade-in">
        <h2 class="section-title">Featured Projects</h2>
        <p class="section-subtitle">A showcase of my technical work</p>
        <div class="projects-grid">
            <div class="project-card">
                <div class="project-icon">🚦</div>
                <h3>Traffic Light Control System</h3>
                <p>A smart automation project that simulates real-time traffic signal control using logical sequencing and embedded programming concepts to regulate vehicle flow efficiently.</p>
            </div>
            <div class="project-card">
                <div class="project-icon">🤖</div>
                <h3>All-Direction Mecanum Wheel Robot</h3>
                <p>A robotic car capable of moving in all directions using mecanum wheels, powered by Arduino and motor control logic for precise mobility and enhanced maneuverability.</p>
            </div>
            <div class="project-card">
                <div class="project-icon">🌐</div>
                <h3>Responsive Websites Collection</h3>
                <p>A set of modern, responsive websites designed with HTML, CSS, and JavaScript, optimized for clean UI, smooth animations, and mobile-first performance.</p>
            </div>
        </div>
    </section>

    <!-- ==================== CONTACT SECTION ==================== -->
    <section id="contact" class="fade-in">
        <h2 class="section-title">Get In Touch</h2>
        <p class="section-subtitle">Let's connect and collaborate</p>
        <div class="contact-container">
            <div class="contact-card">
                <div class="contact-icon">📧</div>
                <h3>Email</h3>
                <a href="mailto:Parikshitchauhan642@gmail.com">Parikshitchauhan642@gmail.com</a>
            </div>
            <div class="contact-card">
                <div class="contact-icon">📱</div>
                <h3>Instagram</h3>
                <a href="https://instagram.com/Parikshit.01" target="_blank">@Parikshit.01</a>
            </div>
        </div>
    </section>

    <!-- ==================== FOOTER ==================== -->
    <footer>
        <p>&copy; 2025 Parikshit Chauhan. All rights reserved. Built with passion and precision.</p>
    </footer>

    <!-- ==================== JAVASCRIPT ==================== -->
    <script>
        // Theme Toggle Functionality
        const themeToggle = document.getElementById('themeToggle');
        const html = document.documentElement;

        // Check for saved theme preference or default to light mode
        const currentTheme = localStorage.getItem('theme') || 'light';
        html.setAttribute('data-theme', currentTheme);
        themeToggle.textContent = currentTheme === 'dark' ? '☀️' : '🌙';

        themeToggle.addEventListener('click', () => {
            const currentTheme = html.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            themeToggle.textContent = newTheme === 'dark' ? '☀️' : '🌙';
        });

        // Mobile Menu Toggle
        const menuToggle = document.getElementById('menuToggle');
        const navLinks = document.getElementById('navLinks');

        menuToggle.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        // Close mobile menu when clicking on a link
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
            });
        });

        // Scroll Animation Observer
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        // Observe all sections with fade-in class
        document.querySelectorAll('.fade-in').forEach(section => {
            observer.observe(section);
        });

        // Smooth scroll behavior for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html>
